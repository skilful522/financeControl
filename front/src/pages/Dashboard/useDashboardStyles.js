import { makeStyles } from '@material-ui/core';

export const useDashboardStyles = makeStyles(() => ({
  tableWrapper: {
    height: '100%',
    width: '100%',
  },
  textFieldWrapper: {
    padding: '15px 0',
  },
}));
